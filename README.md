# AIA Automation

Scripts for:

-   Fetching IA agent data
-   Dynamically grabbing CAPTCHA image/audio

## Setup

cd python
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
playwright install

## Scripts

-   scripts/fetch_agents.py
-   scripts/grab_captcha.py
